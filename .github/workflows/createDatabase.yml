name: createDatabase
run-name: setting up the new database
on:
  workflow_dispatch
jobs:
  test-output:
    runs-on: ubuntu-latest
    steps:
      - name: Test output
        run: echo "here is the test"